export interface PointGroupOptions {
    dotSize: number;
}

export interface PointGroupOptions {
    size: number;
}

export interface Options extends Partial<PointGroupOptions> {
    minDistance?: number;
}
